import placesJSON from "./places.json"
import { getUniqueArrayOfObjects } from "./utils/getUniqueArrayOfObjects"
import fs from "fs"

// function mapPlaces() {
// console.log(placesJSON.length);
// const uniquePlaces = getUniqueArrayOfObjects(placesJSON, 'place_id');
// console.log(uniquePlaces.length);
// fs.writeFileSync('./places.json', JSON.stringify(uniquePlaces, null, 2));
// }

// mapPlaces();
